PORT=8787

# Relative paths are resolved from this app repository root.
# Use an absolute path here if you want to keep the content repo elsewhere.
CONTENT_ROOT=wiki-knowledge

# DATA_DIR defaults to CONTENT_ROOT/.wiki
# DATABASE_PATH defaults to DATA_DIR/wiki.sqlite
